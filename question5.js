/* // FizzBuzz: Write a program that prints numbers from 1 to N. But for multiples of 3, print "Fizz" instead of the number, and for the multiples of 5, print "Buzz." For numbers which are multiples of both 3 and 5, print "FizzBuzz."
const UptoN = (N) => {
  for (i = 1; i <= N; i++) {
    if (i % 3 === 0 && i % 5 === 0) {
      console.log("fuzzbuzz");
    } else if (i % 3 === 0) {
      console.log("fuzz");
    } else if (i % 5 === 0) {
      console.log("buzz");
    } else {
      console.log(i);
    }
  }
};

UptoN(100);
var hool = 90;

var hool = 9;

console.log(hool);

 */

/* const MinandMax = (arr) => {
  // let min = arr.min();
  /*   let max = Math.max.apply(null, arr);
  let min = Math.min.apply(null, arr); 
  let max = Math.max(...arr);
  let min = Math.min(...arr);

  console.log(max);
  console.log(min);
};
*/
/* const RevStr = (str) => {
  let reverse = "";
  for (let i = str.length - 1; i >= 0; i--) {
    reverse += str[i];
  }
  return reverse;
};
let str = "hello world";
console.log(RevStr(str));

const arr = [21, 2, 12, 1, 2, 12, 1, 21, 2, 12, 1];
let reverse = arr.sort().reverse();
console.log(reverse);


 */

let str = ["hello", "world", "match"];
// const SplitStr = [];
/* 
for (const arr of str) {
  console.log(arr);
  const characters = arr.split("");
  console.log(characters);
  SplitStr.push(...characters);
}
 */
// console.log(SplitStr);
